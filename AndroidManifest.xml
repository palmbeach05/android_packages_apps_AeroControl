<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.aero.control"
    android:versionCode="33"
    android:versionName="3.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.ACCESS_SUPERUSER" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
	<uses-permission android:name="android.permission.ACCESS_RESTRICTED_SETTINGS" />
	<uses-permission android:name="android.permission.READ_DEVICE_CONFIG"
        tools:ignore="ProtectedPermissions" />

    <application
        android:theme="@style/AeroTheme"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher">

        <activity
			android:theme="@style/Theme.Transparent"
            android:label="@string/app_name"
            android:name="com.aero.control.SplashScreen"
            android:screenOrientation="portrait"
            android:configChanges="screenSize|screenLayout|orientation" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="@string/app_name"
            android:name="com.aero.control.AeroActivity"
            android:launchMode="singleTop"
            android:configChanges="screenSize|screenLayout|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:theme="@style/RebootDialog"
            android:name="com.aero.control.boot.RebootActivity"
            android:excludeFromRecents="true" />

        <activity
            android:name="com.aero.control.settings.PrefsActivity"
            android:configChanges="screenSize|screenLayout|orientation">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.aero.control.AeroActivity" />
        </activity>

        <activity
            android:name="com.aero.control.settings.AboutActivity"
            android:configChanges="screenSize|screenLayout|orientation">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.aero.control.settings.PrefsActivity" />
        </activity>

		<receiver android:name="com.aero.control.boot.bootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:name="com.aero.control.boot.bootService"
            android:enabled="true">
        </service>
        <service
            android:name="com.aero.control.service.PerAppService"
            android:enabled="true">
        </service>

    </application>

</manifest>
